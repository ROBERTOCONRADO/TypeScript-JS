"use strict";
let chave;
// let chave: "nome" | "preco" | "novo";
chave = "novo";
function coordenas(x, y) {
    return { x, y };
}
let coord;
coord = (x, y) => {
    return {
        x,
        y,
    };
};
